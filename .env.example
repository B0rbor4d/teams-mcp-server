# ===========================================
# Azure App Registration (ERFORDERLICH)
# ===========================================
# Zugangsdaten aus dem Azure Portal
AZURE_CLIENT_ID=your-client-id-here
AZURE_CLIENT_SECRET=your-client-secret-here
AZURE_TENANT_ID=your-tenant-id-here

# ===========================================
# Authentifizierungsmodus
# ===========================================
# Mögliche Werte: application | delegated | user
#
# application: App agiert als eigene Identität (Bot/Service)
#              → Nachrichten erscheinen vom App-Namen
#              → Benötigt Application Permissions in Azure
#              → Empfohlen für MCP Server
#
# delegated:   App agiert im Namen eines angemeldeten Benutzers
#              → Nachrichten erscheinen vom Benutzernamen
#              → Benötigt Delegated Permissions in Azure
#              → Für interaktive Anwendungen
#
# user:        App agiert als spezifischer Benutzer
#              → Wie delegated, aber mit fester User-ID
#              → Benötigt AUTH_USER_ID
AUTH_MODE=application

# User-ID für AUTH_MODE=user (optional)
# Die Azure AD Object-ID des Benutzers
# AUTH_USER_ID=12345678-1234-1234-1234-123456789abc

# ===========================================
# Nachrichten-Konfiguration
# ===========================================
# Display Name für Bot (nur bei AUTH_MODE=application)
# Wird als Absendername angezeigt
BOT_DISPLAY_NAME=Teams MCP Server

# Signatur zu Nachrichten hinzufügen? (true/false)
# Fügt automatisch eine Signatur am Ende jeder Nachricht hinzu
MESSAGE_ADD_SIGNATURE=false

# Benutzerdefinierte Signatur (optional)
# Falls leer, wird Standard-Signatur verwendet
# MESSAGE_SIGNATURE=\n\n---\n🤖 Automatisch gesendet via MCP Server

# ===========================================
# Teams Bot Configuration (OPTIONAL)
# ===========================================
# Nur erforderlich für erweiterte Bot-Funktionalität
# (z.B. proaktive Nachrichten, Bot-Befehle)
# BOT_ID=your-bot-id-here
# BOT_PASSWORD=your-bot-password-here
